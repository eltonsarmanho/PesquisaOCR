# ===========================================
# DEPENDÊNCIAS OCR - PESQUISAOCR
# ===========================================
# pip3 install torch torchvision --index-url https://download.pytorch.org/whl/cu126
# ============ CORE DEPENDENCIES ============
numpy>=1.24.0,<2.0.0
opencv-python>=4.8.0,<5.0.0
pdf2image>=1.16.0
Pillow>=10.0.0
python-dotenv>=1.0.0

# ============ OCR LIBRARIES ============
# PyTesseract (sempre funciona)
pytesseract>=0.3.10

# EasyOCR com PyTorch CPU-only (mais estável)
easyocr>=1.6.0
# torch>=2.0.0,<3.0.0 --index-url https://download.pytorch.org/whl/cpu
# torchvision>=0.15.0,<1.0.0 --index-url https://download.pytorch.org/whl/cpu

# ============ CLOUD APIs ============
# Google Cloud
google-cloud-vision>=3.4.0
google-generativeai>=0.7.0
google-auth>=2.17.0
google-auth-oauthlib>=1.0.0

# AWS
boto3>=1.28.0
botocore>=1.31.0

# ============ DATA ANALYSIS ============
pandas>=2.0.0,<3.0.0
matplotlib>=3.7.0,<4.0.0
seaborn>=0.12.0,<1.0.0
scikit-learn>=1.3.0,<2.0.0

# ============ TEXT EVALUATION ============
jiwer>=3.0.0  # Word Error Rate
python-Levenshtein>=0.20.0  # String similarity/distance
Levenshtein>=0.20.0  # Alternative implementation

# ============ STATISTICAL ANALYSIS ============
scipy>=1.10.0,<2.0.0
statsmodels>=0.14.0
scikit-posthocs>=0.7.0  # Post-hoc statistical tests

# ============ UTILITIES ============
tqdm>=4.65.0  # Progress bars
pathlib2>=2.3.0  # Better path handling (Python 3.4+ compatibility)

# ===============GPU com CUDA 12.1 (recomendado)============================
# pip uninstall -y torch torchvision torchaudio
# pip cache purge
# pip install --index-url https://download.pytorch.org/whl/cu121 torch torchvision torchaudio -U

# ================PaddleOCR===========================
# 
# Opção 1: Instalação com Suporte a GPU (recomendado se você tiver NVIDIA)
# python -m pip install paddlepaddle-gpu==3.1.0 -i https://www.paddlepaddle.org.cn/packages/stable/cu126/
# pip install paddleocr
# Opção 2: Instalação com Suporte a CPU (mais leve, mas mais lenta)
# python -m pip install paddlepaddle==3.1.0 -i https://www.paddlepaddle.org.cn/packages/stable/cpu/
# pip install paddleocr
# ===========================================

# ============ OPTIONAL PACKAGES ============
# Uncomment if needed:
# jupyter>=1.0.0
# ipykernel>=6.20.0
# notebook>=6.5.0

# ============ REMOVED/PROBLEMATIC ============
# paddleocr - dependency conflicts with other packages
# paddlepaddle - conflicts with TensorFlow/PyTorch
# calamari-ocr - old TensorFlow version conflicts
# openai - not needed for OCR tasks
# opencv-contrib-python - conflicts with opencv-python
# dateparser - not essential for OCR

